from core.oj import oj

# users_probs data structure:
#   tuple(users, probs)
#   users:
#     dict(user1_name: {oj1: oj1_uid, ...}, ...)
#   probs:
#     [prob1_namestr, prob2_namestr, ...]
# return data structure:
#   dict(user1_name: [prob1_status_str, prob2_status_str, ...], ...)
def fetch(users_probs):
	pass
